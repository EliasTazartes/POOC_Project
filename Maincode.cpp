#include <iostream>
#include <cmath>
#include <iomanip>
#include <fstream>
#include <string>

using namespace std;

int main()
{
  string firstName, lastName;
  int frequency;
  int loanDuration;
  int periodNb;
  periodNb = loanDuration * frequency;
  double loanPV;
  double APR;
  double periodicRate;
  periodicRate = APR / frequency;
  double principal, interestPayment, instalment;
  
  
  cout << "Welcome to Sorbank, a bank for and by Sorbonne students !" <<endl;
  cout << "This app is designed to generate with your help your loan payment schedule !";
}
